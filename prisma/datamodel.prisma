type Link {
  id: ID! @unique
  createdAt: DateTime!
  description: String!
  url: String!
  postedBy: User
}

type User {
  id: ID! @unique
  name: String! @unique
  email: String! @unique
  password: String!
  links: [Link!]!
}
